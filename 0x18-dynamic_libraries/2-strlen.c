#include "main.h"

/**
 * _strlen - Calculate the lenght of a string.
 * @s: Pointer that point to a string
 * Return: Lenght of Str
*/

int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{

}
return (i);
}
